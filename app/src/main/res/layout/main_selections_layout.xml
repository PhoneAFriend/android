<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/my_grey"
    tools:context="seniordesign.phoneafriend.main_screen">

        <Button
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:text="Look for Users"
            android:id="@+id/search_button"
            android:background="@drawable/custom_button"
            android:layout_alignParentTop="true"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="50dp"/>

        <Button
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:text="Look Through Posts"
            android:id="@+id/main_screen_posts_button"
            android:background="@drawable/custom_button"
            android:layout_below="@+id/search_button"
            android:layout_marginTop="50dp"
            android:layout_centerHorizontal="true"/>

        <Button
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:text="My Posts"
            android:id="@+id/button4"
            android:background="@drawable/custom_button"
            android:layout_below="@+id/main_screen_posts_button"
            android:layout_marginTop="50dp"
            android:layout_centerHorizontal="true"/>

        <Button
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:text="Start A Session"
            android:id="@+id/start_session_button"
            android:background="@drawable/custom_button"
            android:layout_below="@id/button4"
            android:layout_marginTop="50dp"
            android:layout_centerHorizontal="true"/>

</RelativeLayout>
